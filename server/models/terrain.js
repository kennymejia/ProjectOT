//forest    plains  mountains   dessert
var myArray = [forest, plains, mountains, dessert];

while (currentPace != resting ) {
    var randomTerrain = myArray[Math.floor(Math.random() * myArray.length)];
}